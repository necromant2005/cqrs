parameters:

services:
  _defaults:
    autowire: true
    autoconfigure: true

  App\:
    resource: '../src/'

  App\Security\WebhookSecretVerifier:
    arguments:
      $secret: '%env(BILLING_WEBHOOK_SECRET)%'
